# FinanceAgentV2.0 依赖清单
# 安装时请按下方说明分两步执行，以确保使用 CUDA 版 PyTorch
#
# Step 1: 安装 CUDA 版 PyTorch（需要 NVIDIA GPU + CUDA 12.8）
#   pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Step 2: 安装其余依赖
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# 若无 GPU，Step 1 可替换为：
#   pip install torch==2.7.0 torchvision torchaudio
# ─────────────────────────────────────────────────────

certifi==2026.6.17
numpy==1.26.4
openai>=2.44.0
pandas==2.3.3
pandas-ta==0.4.19b0
pillow==12.2.0
plotly==6.8.0
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.14.6
streamlit==1.58.0
streamlit-autorefresh==1.0.1
transformers==4.51.3
yfinance==0.2.66
